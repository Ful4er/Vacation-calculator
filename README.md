# VacationCalculator

**Калькулятор отпускных** — приложение на **Spring Boot (Java 11)** для расчёта отпускных с учётом средней зарплаты и (опционально) с учётом выходных и праздничных дней через внешний календарный API.

## 🧠 Описание

Приложение предоставляет REST API:

- Базовый расчёт отпускных по средней зарплате (`avgSalary`) и количеству дней (`days`);
- Расчёт с указанием даты начала отпуска (`startDate`) с учётом выходных и праздничных дней.

---

## 🚀 Установка и запуск

1. Склонируй репозиторий:
   ```bash
   git clone
   ```
2. Перейди в директорию проекта:
   ```bash
   cd VacationCalculator
   ```
3. Собери и запусти проект:
   ```bash
   mvn clean package
   mvn spring-boot:run
   ```
Сервис будет доступен по умолчанию на: `http://localhost:8080`

## 📍 API

### 🔹 GET `/calculate`

**Параметры:**

| Параметр     | Тип         | Обязательный | Описание                                     |
|--------------|-------------|--------------|----------------------------------------------|
| `avgSalary`  | `BigDecimal`| Да           | Средняя зарплата за 12 месяцев               |
| `days`       | `Integer`   | Да           | Количество дней отпуска                      |
| `startDate`  | `LocalDate` | Нет          | Дата начала отпуска (`yyyy-MM-dd`)           |

## 📤 Примеры запросов
### Базовый расчёт:
```bash
GET /calculate?avgSalary=60000&days=10
```
### С учётом выходных и праздников:
```bash
GET /calculate?avgSalary=60000&days=10&startDate=2026-01-15
```
## 📦 Пример ответа
```json
{
  "total": 20548.12,
  "paidDays": 8
}
```
## 📊 Логика расчёта
### 📌 Базовый расчёт
Если параметр startDate не указан, расчёт производится по формуле:
```ini
dailyRate = avgSalary / 29.3
total = dailyRate × days
```
Где:

- `29.3` — усреднённое количество рабочих дней в месяце;
- Итоговое значение округляется до 2 знаков после запятой (HALF_UP).
## 📌 Расчёт с датой начала
1. Если указан startDate:
2. Формируется диапазон дат длиной days, начиная с startDate.
3. Для каждой даты выполняется запрос к внешнему календарному API.
4. Считаются только рабочие дни.
5. На основе реального количества рабочих дней (paidDays) считается итоговая сумма отпускных:

```ini
dailyRate = avgSalary / 29.3
total = dailyRate × paidDays
```
## 🧪 Тесты
В проекте включены юнит‑тесты:

### 📍 VacationServiceTest

Проверяет:
- корректность базового расчёта;
- обработку ошибок;
- работу расчёта с датой начала и внешним API (через мок).

### 📍 CalendarApiServiceTest

Проверяет:
- работу REST‑клиента для обращения к календарному API;
- корректность обработки ответов;
- поведение при ошибках внешнего сервиса.
### ⚙️ Конфигурация
## 📌 application.properties
```properties
spring.application.name=VacationCalculator

# Внешний календарь для выходных/праздничных дней
calendar.api.base-url=https://calendar.kuzyak.in/api/calendar
```
## ❓ Обработка ошибок
| Ситуация                           | Исключение                 |
| ---------------------------------- | -------------------------- |
| Отсутствуют обязательные параметры | `BadRequestException`      |
| Внешний API вернул ошибку          | `ExternalServiceException` |

## ⚡ WebClient и реактивные запросы

Для обращения к внешнему календарному API используется **Spring WebClient**.  
Запросы выполняются **реактивно** через `Mono`/`Flux`, что позволяет неблокирующе собирать данные о рабочих днях.

Пример конфигурации:

```java
@Bean
public WebClient calendarWebClient(@Value("${calendar.api.base-url}") String baseUrl) {
    return WebClient.builder()
            .baseUrl(baseUrl)
            .build();
}
```
Использование в сервисе:
``` java
Mono<Long> paidDaysFlux = Flux.range(0, days)
        .map(startDate::plusDays)
        .flatMap(calendarApiService::isWorkingDay)
        .filter(Boolean::booleanValue)
        .count();
```
